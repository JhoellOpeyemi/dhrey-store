import styled from "styled-components";

export const FaqsWrapper = styled.section`
  padding: 6rem 0;
  border-bottom: 1px solid ${({ theme }) => theme.colors.black4};
`;

export const FaqsGroup = styled.ul`
  margin: 3.75rem auto 0;
  max-width: 60rem;
`;
